variables:
      VERSION: 7.2
pool:
  vmImage: "ubuntu-18.04"
  workingDir: '$(service_path)'

steps:

  - script: echo $(dev)

  - script: npm install
  
  - script: npm install @playwright/test
  - script: npx playwright install 

    displayName: "Install browser dependencies"
  - script: npm i playwright-chromium

    displayName: "run test"
  - script: npm run azure2
      
  - task: CopyFiles@2
    inputs:
      #sourceFolder: '$(System.DefaultWorkingDirectory)/_daniavander_hydro-test/playwright-report'
      targetFolder: '$(Build.ArtifactStagingDirectory)'
      
  - task: PublishBuildArtifacts@1    
    displayName: 'Publish Artifact: drop'
    inputs:
      PathtoPublish: '$(build.artifactstagingdirectory)'

